{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug lldb-dap",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${workspaceFolder}/bin/Odin_MiniRPG",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build debug",
            "initCommands": [
                "settings set target.disable-aslr false"
            ]
        }
    ]
}